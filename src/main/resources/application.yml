server:
  port: ${port:8000}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
      force-request: true
      force-response: true
  tomcat:
    uri-encoding: UTF-8
    
spring:
  mvc:
    static-path-pattern: /static/**
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/coinfiesta?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true
    username: root
    password: CoinFiesta96*
  servlet:
    multipart:
      max-file-size: 50MB        
      max-request-size: 50MB
          
mybatis:
  mapper-locations:
  - /mappers/*.xml