<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.CoinFiesta.CoinFiesta.testRep">
	
	<select id="test" resultType="Integer" parameterType="Integer">
		select * from number where id = 2;
	</select>
</mapper>